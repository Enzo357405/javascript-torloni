let numero = parseInt(prompt("Digite um numnero para saber c e impar ou par:"));

if (numero % 2 === 0) {
    alert("O número " + numero + " é Par.");
} else {
    alert("O número " + numero + " é Ímpar.");
}